"""
LightRAG MCP Server — Model Context Protocol adapter for LightRAG.

Wraps the LightRAG REST API as an MCP stdio server so that
MCP-compatible agents (Claude Code, Hermes, etc.) can interact
with the RAG knowledge base through standardized tools, resources,
and prompts.
"""
